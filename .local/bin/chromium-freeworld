#!/bin/sh
# Wrapper for chromium-freeworld that includes a few extra flgas

echo "$@"

/usr/bin/chromium-freeworld --disable-hyperlink-auditing --force-dark-mode --enable-features=WebUIDarkMode,VaapiVideoDecoder "$@"
